{# app/templates/ui/steps/common/_calendar.html.j2 — Étape Calendrier du contrat (commune) #}
<div class="step card" data-step="{{ step_num|default('4') }}" aria-hidden="true">
  <h3>{{ "%02d"|format(step_num|default(4)) }}. Calendrier du contrat</h3>

  <div class="grid2">
    <div>
      <label>Date de début du contrat
        <span class="help" title="Précisez la date exacte de prise de fonction. Elle détermine le début de l'ancienneté ainsi que le point de départ de la période d'essai.">i</span>
      </label>
      <input type="date" name="contract_start" id="contract_start" data-required="true">
    </div>
    <div></div>
  </div>

  <div class="hintbar" style="margin-top:10px">
    💡 Cette date sert de référence pour l’ancienneté, l’essai et certains calculs conventionnels.
  </div>

  <hr class="divider">

  <!-- Ancienneté (reprise éventuelle) -->
  <h4 style="margin:0 0 6px">Ancienneté (reprise le cas échéant)
    <span class="help" title="0 si embauche initiale. Renseignez uniquement s’il y a reprise d’ancienneté (ou pour simuler des règles dépendant d’un palier).">i</span>
  </h4>
  <div class="grid2" style="margin-top:8px">
    <div>
      <label>Ancienneté — années</label>
      <input id="seniority_years" type="number" min="0" step="1" value="0">
    </div>
    <div>
      <label>Ancienneté — mois</label>
      <input id="seniority_months" type="number" min="0" max="11" step="1" value="0">
    </div>
  </div>

  <hr class="divider">

  <!-- Entretien professionnel — périodicité (déplacé depuis l'étape CCN) -->
  <h4>Entretien professionnel — Périodicité</h4>
  <div class="grid2">
    <div>
      <label>Périodicité retenue (facultatif)</label>
      <input name="entretien_periodicity" id="entretien_periodicity" placeholder="tous les deux ans">
      <small class="muted">À défaut de dispositions conventionnelles, la périodicité légale est tous les deux ans.</small>
    </div>
    <div></div>
  </div>

  {% if doc_type|default('cdi') == 'cdd' %}
  <div class="hintbar" style="margin-top:8px">
    L’entretien professionnel s’applique à tous les salariés, quelle que soit la nature du contrat (CDI, CDD, alternance, temps partiel).
    <br>La périodicité de principe est <strong>tous les 2 ans</strong>, calculée <em>date à date</em> à partir de l’entrée dans l’entreprise (puis de l’entretien précédent).
    <br>Un accord collectif peut prévoir une périodicité différente (ex. annuelle).
    <br><br>Si le CDD n’atteint pas l’échéance (<em>moins de 2 ans d’ancienneté continue</em> dans l’entreprise et aucun retour de congé ouvrant droit pendant le contrat), aucune obligation d’organiser l’entretien — sauf si un accord collectif impose une périodicité plus courte.
    <br>Si des CDD successifs mènent à 2 ans d’ancienneté dans l’entreprise, l’entretien devient exigible à cette date, même si le salarié est en CDD à ce moment‑là.
    <br><a href="https://www.legifrance.gouv.fr/codes/article_lc/LEGIARTI000038610069" target="_blank" rel="noopener">Article L6315‑1</a> — <a href="https://www.service-public.fr/" target="_blank" rel="noopener">service‑public.fr</a>
  </div>
  {% else %}
  <div class="hintbar" style="margin-top:8px">
    L’entretien professionnel s’applique à tous les salariés, quelle que soit la nature du contrat (CDI, CDD, alternance, temps partiel).
    <br>La périodicité de principe est <strong>tous les 2 ans</strong>, calculée <em>date à date</em> à partir de l’entrée dans l’entreprise (puis de l’entretien précédent).
    <br>Un accord collectif peut prévoir une périodicité différente (ex. annuelle).
    <br><a href="https://www.legifrance.gouv.fr/codes/article_lc/LEGIARTI000038610069" target="_blank" rel="noopener">Article L6315‑1</a>
  </div>
  {% endif %}

  {# Extensions visibles uniquement pour CDD: terme et renouvellement #}
  {% if doc_type|default('') == 'cdd' %}
  <hr class="divider">

  <!-- Terme du contrat (CDD) -->
  <h4 style="margin:0 0 6px">Terme du contrat</h4>

  <div class="vlist" style="gap:10px">
    <div class="radio">
      <input type="radio" name="cdd_term_kind" id="cdd_term_fixed" value="fixed">
      <label class="label" for="cdd_term_fixed">Contrat à terme fixe (date de fin connue)</label>
      <span></span>
    </div>
    <div class="radio">
      <input type="radio" name="cdd_term_kind" id="cdd_term_imprecise" value="imprecise">
      <label class="label" for="cdd_term_imprecise">Contrat sans terme précis (lié à un événement)</label>
      <span></span>
    </div>
  </div>

  <div id="cdd_term_fixed_box" class="fieldset" style="display:none; margin-top:10px">
    <div class="grid3">
      <div>
        <label>Date de fin de contrat</label>
        <input type="date" name="contract_end" id="contract_end">
        <small class="muted">La date doit être compatible avec les durées légales et conventionnelles applicables.</small>
      </div>
      <div></div><div></div>
    </div>
  </div>

  <div id="cdd_term_imprecise_box" class="fieldset" style="display:none; margin-top:10px">
    <div class="callout callout-info" style="margin-bottom:8px">
      Sans terme précis : indiquez une durée minimale (L1242‑7). Elle est obligatoire lorsque la date de fin n’est pas connue.
    </div>
    <div class="grid3">
      <div>
        <label>Durée minimale</label>
        <div class="input-with-suffix">
          <input type="number" min="0" step="1" name="cdd_min_duration_value" id="cdd_min_duration_value" placeholder="Ex. 30">
          <span class="suffix"></span>
        </div>
      </div>
      <div>
        <label>Unité</label>
        <select name="cdd_min_duration_unit" id="cdd_min_duration_unit">
          <option value="jours" selected>Jours</option>
          <option value="mois">Mois</option>
        </select>
      </div>
      <div></div>
    </div>
    <div style="margin-top:10px">
      <label>Événement déclencheur de fin du contrat</label>
      <input name="cdd_term_event" id="cdd_term_event" placeholder="Ex. retour de Mme X ; fin de la saison d’été ; achèvement des travaux…">
      <small class="muted">Précisez l’événement objectif mettant fin au contrat sans terme précis.</small>
    </div>
  </div>

  <!-- Hints durée max / carence (CDD) -->
  <div id="cdd_hint_duration_max" class="callout" style="display:none; margin-top:10px"></div>
  {# Carence désormais affichée à l’étape Motif — pas de duplication ici #}

  <hr class="divider">

  <!-- Renouvellement (CDD) -->
  <h4 style="margin:0 0 6px">Renouvellement</h4>
  <div class="grid3 align-top">
    <label class="inline">
      <input type="checkbox" id="cdd_renewable" name="cdd_renewable" value="oui"> Contrat renouvelable
    </label>
    <div>
      <label>Nombre de renouvellements possibles</label>
      <input type="number" min="0" step="1" id="cdd_renewals_max" name="cdd_renewals_max" value="0" disabled>
      <small class="muted">Indiquez le nombre souhaité. Un rappel des plafonds applicables est affiché ci‑dessous.</small>
    </div>
    <div></div>
  </div>
  <div id="cdd_hint_renewals" class="callout" style="display:none; margin-top:8px"></div>

  <div class="callout callout-info" style="margin-top:12px">
    <strong>Transmission du contrat</strong><br>
    Le contrat de travail doit être transmis au salarié au plus tard dans les 2 jours ouvrables suivant l'embauche. (Article L. 1242-13 du Code du travail)
    <br><br>
    Le non-respect de ce délai de transmission n’entraîne pas, à lui seul, la requalification du CDD en CDI. Mais, en l'absence de respect de cette disposition, le salarié a droit à une indemnité, à la charge de l'employeur, égale, au maximum, à un mois de salaire. (Article L. 1245-1 du Code du travail)
  </div>
  {% endif %}

  <div class="actions" style="margin-top:10px">
    <button class="btn" data-prev>Précédent</button>
    <button class="btn-primary" data-next>Suivant</button>
  </div>
</div>
